# 금고와 데이터는 깃에 올리지 않는다 (server/.gitignore에도 있지만 이중 안전장치)
server/.env
server/node_modules/
server/data/

# 구글 서비스 계정 열쇠 파일 (절대 깃에 올리지 않음)
server/google-key.json

# Local Netlify folder
.netlify

# 영상·원본 소재는 깃에 올리지 않는다 (용량 큼 · 파이어베이스/드라이브에 보관)
소재(6월)/
*.mp4
*.mov
*.docx
*.pdf

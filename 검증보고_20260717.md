# 검증보고 2026-07-17 — 지니야빌더 실측

> 규칙 준수: 모든 판정은 **코드를 직접 열어** 확인. 추측 없음. 각 줄에 **파일:줄** 근거.
> 대상 라이브 파일: `deploy/main_server.js`(교육생 서버, 라우트 56개·1084줄), `deploy/genya.html`(교육생 UI·1980줄), `server/server.js`(대표 전용 제니야·라우트 167개·5560줄).

## ★먼저: "공정율"의 정의 (정직하게)

이 보고의 공정율%는 **"코드 배선이 되어 있는가"** 기준이다.
대표님 규칙의 **"1차 통과 = 실제 결과 출력"** 기준으로 보면, 나는 아직 **브라우저로 12단계를 한 번도 끝까지 돌려보지 않았다.**
→ 따라서 **런타임 통과율은 "모름"**이고, 아래 %는 전부 **정적(코드) 기준**이다. 이 둘을 섞지 않는다.

---

## 물음 1 — 16개 항목 공정율표 (코드 기준)

| # | 항목 | 목표 | 실제 코드 (파일:줄) | 공정율% | 근거 | 부족한 것 |
|---|------|------|--------------------|--------|------|-----------|
| 1 | 로그인(구글·카카오) | 두 방식 로그인 | genya.html:383-384(버튼) / main_server.js:992(`/auth/google`),1016(callback),1074(`/auth/kakao`),1079(callback) | 85% | UI·서버 라우트·세션쿠키영속(148,1044) 완비 | 실제 로그인 성공 런타임 미검증 |
| 2 | 직업선택→직업별 화면·비서·페인 | 직업마다 다른 화면 | genya.html:414-419(버튼 5+edu), 663-815(GENYA_JOBS), 1010-1038(applyJob 분기) | **40%** | insurance·clinic·realtor·tax·etc 5종만 실데이터 | **★edu(학원원장) 키 없음(663-815) → 1012에서 insurance로 폴백. 7/19 교육생 화면이 틀림** |
| 3 | 설문→프로필 저장 | 입력→저장 | genya.html:425-482(3입력방식) / main_server.js:781(`/api/profile/save`→회원 시트),821(extract),837(chat) | 80% | 폼·대화·업로드 3방식+시트저장 배선 | 저장은 구글 데이터스코프 필요(gateGoogle 782) |
| 4 | 자료 업로드→분석 | 다중 칸칸이+분석 | genya.html:502-524(2슬롯),1927(multiple) / main_server.js:853(`/api/coverage/analyze`),810(drive/token 직행) | 70% | 만능처리기(이미지·PDF·엑셀) 배선 | **다중업로드가 "칸칸이" 아님 — 파일명 합쳐 한 줄(genya.html:1929)** |
| 5 | 도구연결(캘린더·시트·드라이브·Gmail) | 4도구 연결·반영 | genya.html:534-549(4버튼),1760-1763(실API) / main_server.js:1007(connect),240·324·338·353(4 API) | 85% | 최소권한 스코프(1001)·실호출로 상태판정(1766) | 실제 OAuth 왕복 런타임 미검증 |
| 6 | 고객발굴비서 | 실제 리드 표시 | genya.html:1064(패널),1162·1190 / main_server.js:491(`/api/find/leads`),503(reply-draft) | 70% | 유튜브 검색·답글초안·오상열 제외(463) 배선 | **YOUTUBE_API_KEY 없으면 리드 0(494). Render 키 설정 여부=모름** |
| 7 | 증권분석비서 | 증권→분석 문서 | genya.html:627·1530·1907 / main_server.js:599(`/api/policy`)→skills.policy(604)+MEM저장(605) | 80% | 유형판별·보장분석·PDF렌더 배선 | 실제 증권 넣어 결과나오는지 런타임 미검증 |
| 8 | 상품비교추천비서 | 설계서→비교표 | genya.html:1961 / main_server.js:586(`/api/compare`)→skills.compare(591) | 80% | 이미지2장↑ 자동분기·적정성 공식 배선 | 김명란 실측 등 런타임 미검증 |
| 9 | 고객관리비서(카톡발송) | 명단→발송 O/X | genya.html:1213·1233·1259 / main_server.js:624(analyze),634(dashboard),915(초안) | 55% | 명단분석·대시보드·문자초안 배선 | **실제 카톡발송=로컬 genysend:+파워셸(kakao_send_core.py, 대표PC watcher 의존). 서버에 발송 라우트 0. 교육생 PC엔 없음** |
| 10 | 홍보마케팅비서 | 콘텐츠→진단링크 | genya.html:636·1460·1470 / main_server.js:372(`/api/promo/draft`) | 75% | 한줄카피→쇼츠원고 배선 | 발행·진단링크 자동연결 런타임 미검증 |
| 11 | 대시보드(오늘할일·KPI) | KPI·오늘 이벤트 | genya.html:563-568(KPI4),596(브리핑),1823 / main_server.js:634(dashboard) | 60% | 약속·만기 실API 연결 | **발굴 KPI=하드코딩0(1833), 발송 KPI=소스없어0(1835)** |
| 12 | 보상처리비서 | 양식×데이터→발송대기 | **없음** (보상처리/claim 문자열 0건) | **0%** | UI·버튼·라우트·스킬 전무 | 전부 |
| 13 | 결재함비서 | 모닝브리핑·승인·리마인드·일괄다운로드 | genya.html:596(고정라벨),1244(승인모달) | 10% | 승인모달=카톡발송용만 존재 | **독립 결재함 화면 없음·모닝브리핑=고정문구·리마인드/긴급뉴스/일괄다운로드 없음** |
| 14 | 우측 다운로드함(내 자료) | 만든 문서만·PDF등록·삭제 | genya.html:570(버튼),1562-1567(모달),1686-1699(인라인 실다운로드) | 25% | 분석결과 인라인 PDF/CSV 다운로드는 실동작 | **다운로드함 모달 4항목 전부 죽은버튼(toast만, 1564-1567). 상시 "내 자료함" 영역 없음** |
| 15 | 메모리(MEM) 저장·삭제 | 영구기록·삭제버튼 | main_server.js:655(mem/save),661(search),754-758(memory/*) / genya.html:**프론트 버튼 없음** | 20% | 백엔드 저장·검색·삭제 라우트 존재 | **프론트에 사용자용 저장/삭제 버튼 0. localStorage _HIST(1864)만** |
| 16 | 대표/교육생 역할분리 | 역할별 화면 | genya.html:1055(`_isRep` 이메일판정),1275(분기) / 앱분리(genya vs jenya) | 50% | 앱 자체 분리+발굴카드 1곳 이메일분기 | 프론트 분기 지점 1곳뿐(발굴). 나머지 화면 동일 |

**★전체 공정율(코드 기준): 약 56%** — 근거: 16항목 %의 단순평균 = (85+40+80+70+85+70+80+80+55+75+60+0+10+25+20+50)/16 = 890/16 = **55.6%**.
**★전체 공정율(1차 통과=실제 출력 기준): 모름(0에 수렴)** — 12단계를 브라우저로 끝까지 돌린 실측이 아직 없음. 이게 지금 우리 문제의 핵심.

---

## 물음 2 — 교육생 12단계, 어디서 막히나 (코드 기준)

| 단계 | 판정 | 근거(파일:줄) |
|------|------|--------------|
| 1. 대표가 URL 준다 | 통과(대표 행위) | — |
| 2. URL 연다 | 통과 | main_server.js:1144(`/`) |
| 3. 로그인 | 통과(정적)·런타임 모름 | main_server.js:992·1016·1074·1079 |
| 4. 이름·직업 입력 | **보험설계사=통과 / 학원원장=막힘** | genya.html:418(edu버튼) 있으나 GENYA_JOBS(663-815)에 edu 키 없음 → 1012 폴백=보험화면 |
| 5. 설문→프로필 저장 | 통과(정적) | main_server.js:781. 단 구글 데이터스코프 필요(782) |
| 6. 복명복창(에이전트 대화) | 통과(정적) | main_server.js:837(`/api/onboard/chat`)→askClaude |
| 7. 자료업로드→시트+다운로드함 동시반영 | **부분 막힘** | 업로드·분석은 배선(853). 그러나 "다운로드함 동시반영"은 다운로드함이 죽은버튼(genya.html:1564-1567)이라 미충족 |
| 8. 도구연결→메인 반영 | 통과(정적) | genya.html:1760-1763, main_server.js:1007 |
| 9. 고객발굴→실제 리드 | **조건부 막힘** | main_server.js:494 — YOUTUBE_API_KEY 미설정 시 리드 0. Render 설정 여부=모름 |
| 10. 증권분석→다운로드함 등록 | **부분 막힘** | 분석 배선(599). 결과는 인라인 다운로드만, 우측 상시 다운로드함 등록=없음(genya.html:14번 참조) |
| 11. 상품비교→비교표 | 통과(정적)·런타임 모름 | main_server.js:586 |
| 12. 고객관리→카톡 발송·O/X | **막힘(교육생 환경)** | 발송이 로컬 파워셸+watcher(genya.html:1233·1259, 레포밖 kakao_send_core.py) 의존. 교육생 PC엔 없음. 서버 발송 라우트 0 |

**요약:** 코드 배선상 확실히 막히는 곳 = **4단계(학원원장)·9단계(발굴 키)·12단계(카톡발송)**, 부분 막힘 = **7·10단계(다운로드함)**. 나머지는 코드 배선은 있으나 **런타임 통과는 미검증(모름)**.

---

## 물음 3 — 구조 파악 (전부 코드 확인)

**1. `deploy/main_server.js` 라우트 수 = 56개**
명령: `Select-String -Path "deploy\main_server.js" -Pattern 'app\.(get|post|put|delete|patch)\(' | Measure-Object`
출력: `Count : 56`

**2. `server/server.js` 라우트 수 = 167개**
명령: `Select-String -Path "server\server.js" -Pattern 'app\.(get|post|put|delete|patch)\(' | Measure-Object`
출력: `Count : 167`
(server.js는 대표 전용 제니야. 교육생과 별개 앱.)

**3. 구글 도구 연결 시 요청 스코프 — `deploy/main_server.js`에 정의**
- 로그인 스코프 `main_server.js:169` = `openid, email, profile`
- 데이터 스코프 `main_server.js:170` = `calendar.readonly, spreadsheets, drive.readonly, drive.file`
- 도구별 최소권한 `main_server.js:1001`(`CONNECT_SCOPES`) — 도구 누를 때 그 도구 스코프만 누적 요청(incremental, 1011·1014)

**4. 대표와 교육생을 코드에서 어떻게 구분하나**
- **서버(main_server.js): 역할 분기 없음.** `admin|isAdmin|role` grep 0건. 세션은 로그인 이메일별로만 나뉨(sessionOf). 대표 SA 데이터로 폴백하지 않고 "구글 연결 필요"로 게이트(187,190).
- **프론트(genya.html): 이메일 하드코딩 1곳.** `_isRep()`(1055) = 이메일이 `ggorilla11@gmail.com`이면 대표, 아니면 교육생. 실제 분기는 발굴카드(1275) 한 곳뿐(대표=유튜브 시나리오 / 교육생=진단링크).
- **근본 구분 = 앱 자체 분리.** 대표=`server/server.js`(제니야, jenya.onrender.com), 교육생=`deploy/main_server.js`(genya-builder.onrender.com).

---

## 가장 급한 3가지 (7/19 기준)
1. **학원원장 직업 미구현** — GENYA_JOBS에 edu 추가 안 하면 학원원장 교육생에게 보험 화면이 뜬다. (genya.html:663-815, 1012)
2. **12단계 런타임 통과 실측이 0** — 코드는 있어도 실제로 끝까지 흐르는지 아무도 안 돌려봤다.
3. **다운로드함이 껍데기** — 만든 문서가 우측에 안 쌓인다(죽은 toast 버튼). 7·10·시연 흐름에 영향.

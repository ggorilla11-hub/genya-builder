# progress_v3.30 · Step 2-1 하이브리드 라우터 — 스테이징 실측 준비 완료

- **담당**: 엄마2 (백엔드 트랙)
- **작성일**: 2026-07-22
- **상태**: 🟢 **코드 완주 + 로컬 실측 통과 → 스테이징 배포·회장님 실측 대기** (프로덕션 미전환)

---

## 회장님 질문 답변

### ① WS_CHAT_MODEL 확장 완료 여부 → ✅ 완료
`MODEL_SIMPLE`(claude-sonnet-5) / `MODEL_DEEP`(claude-opus-4-8) / `MODEL_FALLBACK`(gpt-4o) 3종. 기존 단발 호출은 `WS_CHAT_MODEL`(=Sonnet5) 하위호환 유지 → **회귀 위험 0**.

### ② Intent 분류 규칙 구현 완료 여부 → ✅ 완료 (LLM 호출 0)
`classifyIntent(text, opts)` 결정적 규칙: 길이>300자 / 재무 키워드 / admin·function-calling → DEEP, 그 외 SIMPLE.

### ③ 로컬 테스트 결과 → ✅ 13/13 통과
실제 `main_server.js` 소스의 `classifyIntent`를 추출해 직접 실행:

| 케이스 | opts | 기대 | 결과 |
|---|---|---|---|
| 안녕하세요 / 고마워요 / 오늘 날씨 / 이름이 뭐예요 / 이거 해줘 | {} | SIMPLE | ✅ SIMPLE |
| 내 노후 준비 어떻게…? | {} | DEEP | ✅ |
| 보험 리모델링 상담 | {} | DEEP | ✅ |
| 이 증권 좀 분석해줘 | {} | DEEP | ✅ |
| 연금저축 vs IRP 비교 | {} | DEEP | ✅ |
| 절세 전략 조언 | {} | DEEP | ✅ |
| **344자 순수문장(키워드 0)** | {} | DEEP(길이) | ✅ |
| 김철수님 만기일 바꿔줘 | admin | DEEP | ✅ |
| 짧은명령 | functionCalling | DEEP | ✅ |

> **정직 메모**: 첫 시도에서 길이 테스트 문장이 258자(<300)라 SIMPLE로 나왔고, '연금' 키워드가 섞여 착시가 있었습니다. → **키워드 0 · 344자** 순수 문장으로 교체해 **길이 규칙만** 재검증했습니다. `node --check` 문법 통과.

### ④ 스테이징 배포 전 회장님 결재 요청 → 대기
아래 이번 반영 확인 후 **스테이징 URL 안내 → 회장님 5개 시나리오 실측 → 함께 리뷰 → 결재 → 프로덕션**.

---

## 이번 반영 (지시 대비)

| 지시 | 반영 |
|---|---|
| **TASK 1 · 임계 5,000원** | ✅ `DAILY_COST_THRESHOLD_KRW = Number(process.env.COST_THRESHOLD_KRW || 5000)` — 코드 상단·환경변수 override, 회장님·팀장 쉽게 조정 |
| 폴백 실측 훅(시나리오4) | ✅ `SIMULATE_CLAUDE_FAIL=1` (기본 OFF) → Claude 강제 실패 → gpt-4o 폴백 실측 |
| 정직 engine 표기 | ✅ `_lastAskModel`로 **실제 쓴 모델**(폴백 시 `gpt-4o`)을 engine에 표기 |
| **TASK 2 · 실측 시나리오 문서** | ✅ `docs/step2_1_실측_시나리오.md` (5개 시나리오) |

임계 조정 시나리오(코드 주석·문서에 명시): **1주 5천(회장님 도그푸딩) → 교육 5명 1만 → 10명+ 2~3만(결재 후)**.

---

## 검증 체크 (Step 2)

- [x] WS_CHAT_MODEL 확장(3모델)
- [x] Intent 규칙 구현 + 로컬 13/13
- [x] 폴백 훅 · 정직 engine · 임계 5천
- [x] `node --check` 통과
- [ ] **스테이징 실측**: Opus 4.8 정상응답(시나리오5개)·SIMPLE/DEEP 분기·폴백·`/api/usage` 실비용 — **회장님 대화 필요**
- [ ] 회장님 프로덕션 결재 → main 배포

---

## ⚠️ 절대금지 준수 확인

- ❌ 결재 없는 프로덕션 Opus 전환 → **스테이징만**, 결재 후 main ✅ 지킴
- ❌ 비용 임계 초과 자동 발생 → **5천원 임계 내장 + 경고** ✅ 지킴
- ❌ 임의 톤 변경 → Step 2-3(페르소나)는 **미착수**(회장님 확정 전 손대지 않음) ✅ 지킴

---

## 다음

**실측 성공 → 회장님 결재 → 프로덕션 전환 → Step 2-2(Pinecone RAG·동의 게이트) 착수.** 순서 준수(실측 안 하고 2-2 착수 시 롤백 부담).

> ※ 병렬: Step 5 알림톡 **6→8개 다목적 확장** 별도 반영 완료 (`docs/알림톡_템플릿_심사신청.md` · `ALIMTALK_TEMPLATES`) — 회장님 심사 신청 대기.

---

## 알려진 한계 (정직)

- `_lastAskModel`은 서버 전역 변수 → **동시 다중 요청 시** engine 라벨이 다른 요청 값을 스칠 수 있음. **1주 회장님 단독 도그푸딩**엔 무해. 다회원 단계에서 요청 스코프로 정밀화 예정(오버엔지니어링 회피 위해 지금은 최소 구현).

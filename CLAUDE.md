# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# 지니야빌더 (genya-builder) — 프로젝트 성경

## 0. 이 문서의 역할

이 파일은 개발팀장(클로드 코드)이 매 세션 시작 때 읽는 "기억"이다. 세션이 끝나도 여기 적힌 것은 사라지지 않는다. 새로 정해지는 규칙·결정은 계속 이 파일에 추가한다.

답변·소통은 **항상 한국어**. 사용자(오상열 대표)는 **비개발자**다. 전문용어 대신 쉬운 말로 설명하고, 코드보다 **"무엇을·왜"** 중심으로 안내한다.

**이 파일의 구성:** 바로 아래 **「현재 상태」**가 지금 어디까지 왔는지·다음에 뭘 할지다(자주 바뀐다). 그 아래 **1~7번과 6-2~6-12**는 사고에서 배운 **원칙·교훈**이다(거의 안 바뀐다. 지우지 않는다).

---

# ★현재 상태 (2026-08-03 사무실 세션 종료) — 매 세션 여기부터 읽는다

## 프로젝트·역할

- **제품**: 지니야빌더 — 1인사업자(보험설계사 등)용 AI 비서 플랫폼. 구글 로그인 + 회원 구글시트(고객 명단·개인정보) 연결.
- **라이브**: https://genya-builder.onrender.com (Render 자동 배포)
- **레포**: GitHub `ggorilla11-hub/genya-builder` (public)
- **역할**
  - **대표(오상열 CEO·CFP)** = 제품·전략 결정. 개발자 아님(복붙·Git Bash로 작업).
  - **CTO(장량)** = 전략 + **모든 엄마 작업을 GitHub 클론에서 코드로 교차검증**. "커밋 ≠ 검증".
  - **엄마 1·엄마 2 (Claude Code)** = 대표 PC에서 실제 코드 작업.
- **OAuth 프로젝트**: 머니야와 **공용**(`moneya-72fe6`·번호 688). ★OAuth 스코프·게시 상태를 건드리면 머니야도 영향받는다. **게시 상태 = 프로덕션(유지)**. 테스트로 바꾸면 refresh token 7일 만료 + 머니야 사고.

## 라이브 상태 (origin/main = 57aaf33)

배포 순서(최근):

```
e801b54  RAG (대표님 2,766청크 · author_knowledge NS)
026ec61  원고 7종·콘텐츠 정리
29c1280·ff7aa4c  A 진입관문 (온보딩→로그인·토큰무효화 구멍)
b34c7d4  C 허용계정 게이트
a421e76  2차 배치1 (날짜·URL·완성원고 7·롱폼·드래그·글자)
dc046a9  2차 배치2 (내자료 듀얼·뉴스레터 편집·원고 표현가드)
57aaf33  배치3-A 이미지·음성 생성 백엔드 (image_gen.js·tts.js)  ← 현재 HEAD
```

**완료·검증됨** (어제 부트캠프 사고 3가지 다 해결)

- **A 진입관문**: URL 접속 → (세션 없으면) 온보딩 아니라 **로그인**. 11(VIP)=메인 / 66·교육생=플래그 있으면 메인. 온보딩 플래그 영구(`genya_onboarding`). 토큰무효화 시 재연결. `/api/diag/onboard-store` ✅
- **C 허용계정 게이트**: 구글+카카오 콜백 둘 다. 명단 시트(`ALLOWLIST_SHEET_ID`)의 이메일만 로그인. **4겹 안전장치**(VIP 항상 통과 · 시트오류→캐시 · 시트ID 없으면 OFF · fail-closed→VIP만) = 대표는 절대 안 잠긴다. SA=`jenya-server@moneya-72fe6.iam.gserviceaccount.com`. `/api/diag/allowlist` ✅ 3명. ★어제 진범 psklby1000 = 명단에 없어 차단(정상 동작).
- **홍보마케팅비서 1차 완료**: 원고 7종(단문·장문·쇼츠원고·카드뉴스원고·오디오원고·이미지원고·롱폼대본) · 콘텐츠 8종(+뉴스레터·롱폼) · 쇼츠 자막영상 · 품질기준서 · RAG · 입구 3개([RAG 자동]·[+내 자료 듀얼]·[+완성원고 직접]).

## 진행 중 — 배치3 이미지·오디오 생성

엄마 2 · 브랜치 `feat/media-gen` · worktree `genya-media`

- **[A] 백엔드 = ★배포 완료(`57aaf33` 라이브)**: `image_gen.js`(gpt-image-1·1024×1536·순차) + `tts.js`(OpenAI tts-1·4096자 분할·조각별 mp3) + `main_server` 2줄. **기존 `OPENAI_API_KEY` 재사용**. CTO 검증 통과.
- **★비용 가드**: 대표(VIP)만 · **서버 판정**(화면 게이트 우회 방지). 기존 일 임계 5,000원(`_usage`)에 집계 · `/api/usage`에서 확인. base64만 · 디스크 0(원칙4).
- **[B] promo_sim = 다음 차례**: 생성 버튼 + 미리보기 실물화(15번). 미리보기는 **오디오 칸만 채우면 된다**(카드뉴스는 이미 진짜 `<img>`).
  ★[B]는 `promo_sim_v8.html`을 만진다 — **한 파일은 한 엄마만**(위 규칙 2). 누가 할지 정하고 시작한다.
- **남은 것**: ffmpeg가 없어 mp3 조각을 못 잇는다(조각별 파일로 나온다). 12번 유튜브 쇼츠는 mp4 변환이 필요하다.

## 다음 할 것

**홍보마케팅 2차** (최종 목표: 하루 1명 이상 전환/결제)

- **배치3** 완성(위)
- **12번 — 다채널 배포**(최대 산·시간 안 쫓김). 발행 현실:
  - ✅ 지금 발행됨: 이메일(gmail)·문자(솔라피)
  - 🟡 심사 후: 카카오 알림톡(템플릿 심사만)·인스타·페북·틱톡·링크드인(앱 심사)
  - 🟡 스코프+mp4 변환: 유튜브 쇼츠(구글 OAuth 있음)
  - 🔴 API 영구 없음(복붙이 정답): 네이버블로그·카페·브런치
  - ★대표: 인스타는 제니야에서 자동발행 됐다고 함 → 12번 때 실제 연결 확인(안 되면 재연결)
  - CTO 추천 순서: 반자동 복사 완성(12-B) → 유튜브 쇼츠(12-A) → 심사 후 자동(인스타 등)
- **13번** 예약 날짜 실시간 연동
- **16번** 구글시트 실연동(노출·유입·고려·전환 측정 · 현재 하드코딩)
- **17번** 홍보 대시보드(전환율·CTR로 하루 1명 최적화)

**미결(홍보 2차에서 남긴 것)**

- 3번 파일 첨부의 **파일 내용 파싱**(현재 UI만 · 고르는 것까지만 됨)
- 2번 진단(소) URL **실주소**(현재 쿼리 `?d=` · 대표가 실주소 주면 경로로)
- 5번 표현가드 **실작동은 대표 확인 몫**(`/api/promo2/*`가 전부 로그인 게이트)

**토큰/로그인 트랙** (개강 후 여유 · 급하지 않음)

- **D 시트 파일 ID 고정**(근본 · 391 vs 11): 이름 검색 4곳(`sheets_crud_skill _loadTableRaw` · `main_server:3767` · `google_connector:24` · `memory_module:27`)을 `resolveRosterId`로. `rosterSheetId` durable 저장. **데이터 뿌리 변경 → 복명복창 필수**. B(`TOKEN_ENC_KEY`)는 이미 해결됨(지반 확보).
- **E** 명단 삭제 기능
- **F** OAuth 프로덕션 인증심사(CASA · `gmail.readonly`/`drive.readonly` restricted · 수 주). 프로덕션이라 급하진 않다.
- 별도: `genya.html:1307 _isRep()` vs 서버 `VIP_EMAIL` 이중관리 정리 · 이메일 하드코딩 전수점검.

**진단 URL**(로그인 후)

`/api/diag/conn`(토큰·이메일) · `/api/diag/token-store` · `/api/diag/onboard-store` · `/api/diag/allowlist` · `/api/roster/list` · `/api/usage`(비용)

## ★절대 규칙 (엄마 필독)

**워크플로우**

1. **복명복창 → CTO 승인 → 작업.** 질문만으로 승인 간주 금지.
2. **한 파일은 한 엄마만.** 두 엄마가 같은 파일(특히 `promo_sim`) 동시 작업 = 충돌.
3. **별도 브랜치 · `git pull` 먼저 · fast-forward.** (git merge 정상화됨 · 잠금 잔재 정리 완료)
4. **"만들었습니다" 금지 → 실측 통과만.** 라이브 화면에서 직접 조작해 확인.
5. **롤백 지점 = 지시값 말고 ★실제 직전 커밋을 눈으로 확인**(과거에 `5072618` 오지시 → 실제는 `e801b54`였다).
6. **`git add -A` 금지.** 파일 이름을 지정해서만 add(다른 엄마 작업이 딸려 들어간다).
7. **커밋 메시지는 코드블록 밖에**(대표가 코드블록은 복사 버튼으로 통째 복사한다).

**🔴 건드리지 마 (danger zone)**

- 생성엔진 원고로직(`promo_skill` 원고생성) · RAG(`author_rag` · `author_knowledge` NS만) · source 통로
- A(진입관문 `/api/boot` 4갈래) · C(허용계정 게이트 `login_allowlist`)
- 22블록(사고방지) · 격리의 `'me' in owners` · 발송(`_sendSmsFor`·`_sendGmailFor`) · 신청캡처 · 음성 · 쇼츠 렌더 · facts/환각차단 · VIP(11)

**검증**

- CTO가 GitHub 클론에서 코드로 교차검증 후 배포 승인. 대표는 팀장·엄마 사이 전달자가 아니다.
- 가짜 응답으로 **고장 상황까지 재현**해 시험한다(진짜로만 하면 오류 때 뭐가 되는지 못 본다).
- 회귀: **정적 파일은 라이브 스모크 8개** / **백엔드 변경은 회귀 107 + A 23 + C 24**.

## 인프라·키

- **Render**: `genya-builder`(Node.js·자동배포). 환경변수: `GOOGLE_OAUTH_*` · `KAKAO_*` · `SOLAPI_*`(문자·알림톡) · `YOUTUBE_API_KEY`(조회용·업로드 아님) · `OPENAI_API_KEY` · `PINECONE_API_KEY` · `TOKEN_ENC_KEY`(토큰 영속·해결됨) · `ALLOWLIST_SHEET_ID`.
- **서비스계정 2개**: `GOOGLE_SA_JSON`=jenya-server(시트 공유된 쪽) / `GOOGLE_SERVICE_ACCOUNT_JSON`=genya-sheet-reader(공유 안 됨). ★게이트는 두 SA 모두 시도한다.
- **Pinecone**: `genya-knowledge`(약관) · `ohwant-genya`(개인기억 `owner_*`) · `author_knowledge`(대표 2,766청크 RAG) — 앞의 둘은 홍보에서 안 연다.
- **원칙4(제로 인그레스)**: 고객 자료·생성물을 서버 디스크에 안 쌓는다. 브라우저로 바로 다운로드.

---

# 이하 원칙·교훈 (거의 안 바뀐다 · 지우지 않는다)

## 1. 프로젝트가 무엇인가

오원트금융연구소의 핵심 상품. **"비개발자를 위한 AI 에이전트 만들기 부트캠프"**의 결과물이다.

**지니야빌더** = 고객이 자기 직업·고민·목표를 입력하면, 그 사람 맞춤형 AI 에이전트("지니야")를 자동으로 만들어 주는 시스템(공장). 고객은 지니야를 통해 자기 업무(문서작성·고객발굴·고객관리 등)를 해결하고, 그 대가로 교육비/구독료를 낸다.

## 2. 핵심 구조 (붕어빵 틀 원리)

모든 지니야는 **같은 틀(골격)**을 쓰고, 고객마다 **"정체성·지식·손"**만 갈아 끼운다.

- **정체성(시스템 프롬프트):** 직업·목표·말투
- **지식(RAG):** 그 업종 자료
- **손(도구):** 그 직업에 필요한 기능

**화면 흐름:**
1. 온보딩(이름 → 직업 → 고민 → 목표)
2. 첫 대화(지니야가 이름 부르며 인사)
3. 홈 대시보드(좌측 메뉴 + 중앙 작업공간 + 우측 무기고)
4. 음성/텍스트로 일 시키기
5. 새 작업은 새 능력으로 무기고에 저장

## 3. 지니야의 성격 (제품 말투)

70대 할머니 행정사, 설계사도 알아듣게 말한다. **클로드/클로드코드 같은 단어는 절대 쓰지 않는다.** 고객은 "지니야"라는 친근한 비서만 만난다. 고객이 막막하게 물으면 쉽게 되묻고, 두세 가지만 받아서 처리한다.

정부 서류 등 중요한 결과물은 항상 **"제출 전 검토하세요"**라고 안내한다(사람이 최종 확인 = 휴먼인더루프).

## 4. 절대 원칙

- 기존 자산(`financial-house-building`/AI머니야, `moneya-server`, `zenya-discord-bot`, 제니야 작업공간)은 **절대 건드리지 않는다.** 지니야빌더는 이 `genya-builder` 폴더에서 완전히 새로 만든다.
- **한 번에 한 단계씩.** 큰 변경 전에는 무엇을·왜 바꾸는지 먼저 설명하고 허락을 받는다.
- **영문 이름 규칙:** 서버 인프라 = `jenya` / 대표 총괄(제니야: 오상열 대표의 지니야, 모든 지니야를 총괄하는 최상위) = `zenya` / 일반 고객 에이전트(지니야) = `genya`.

## 5. 진행 순서 (★역사 기록 — 지금 할 일은 맨 앞 「현재 상태」를 본다)

> ⚠️ 아래 "지금 여기"는 **2026-06-07 시점**의 표시다. 그 뒤로 많이 나아갔다.
> **오늘 무엇을 할지는 이 문서 맨 앞 「★현재 상태」의 "진행 중"·"다음 할 것"을 보라.**

**(2026-06-07 결정) 이번 24시간은 "고객용 지니야"가 아니라 "대표 전용 제니야"를 먼저 만든다:**

1. (완료) 백서(docs/)·에이전트 문서(agents/) 채우기
2. **제니야 화면 골격:** 좌측 에이전트 / 중앙 회의·대화 / 우측 도구 — 구글 로그인 없이 대표 전용으로 간단히
3. **제니야 총괄 + 7개 에이전트 두뇌 연결:** 대화·호명·회의 작동 (이게 핵심)
4. 여유되면 음성
5. 교육생용 온보딩·구글 로그인·웹주소 배포는 그 다음 단계

**현재 제니야는 3단계(두뇌 연결)까지 완료: 화면+서버+에이전트 정체성+호명+회의+정기보고+저장. 배포 완료: https://jenya.onrender.com (Render, 깃허브 genya-builder 저장소 푸시 시 자동 재배포). 자세한 진행은 docs/진행기록.md 참고.**

**그 이후 (기존 계획):**

1. (완료) 엄마 클로드 코드 설치 + 이 프로젝트 성경 작성
2. **지니야빌더 제작:** 온보딩 화면 → 개인별 홈페이지(대시보드+무기고) → 지니야 시스템 프롬프트
3. **도그푸딩:** 오상열 대표 본인 지니야로 테스트 → 1호 행정사 파일럿(특별가) → 직업별 한 명씩 검증(도장깨기)
4. 검증 후 **빌드인퍼블릭** → 부트캠프 1기 모집(날짜는 검증 후 확정)

## 6. 사업 메모

- **수익 구조:** 부트캠프 교육비(일시불) + 지니야 이용료(월 구독). 정확한 금액은 도그푸딩으로 1인당 실제 비용을 실측한 뒤 정한다.
- **목표:** 보험설계사 70만 명의 필수과정. 단, 1기는 **8명 깊은 만족**부터.

## 6-2. 불변 원칙 — 절대 준수 (2026-07-27 확정)

1. **복명복창 → 승인 → 착수.** 승인 없이 작업하지 않는다.
2. **하나씩 실물 확인(건바이건).** 여러 개를 한꺼번에 밀지 않는다.
3. **발송은 대표님 승인만. 자동 발송 0.**
4. **제로 인그레스.** 개인정보는 서버에 저장하지 않는다.
5. **회색지대 안 한다.** GUI 자동화·크롤링 금지. 합법 공식 API만.
6. **환각 금지.** 지어내지 않는다. 화면에 실제로 있는 데이터만 쓴다.
7. **기존 기능 22블록 보존.** 새 기능은 **추가·호출만**.

## 6-3. 기존 기능 22블록 — 코드 줄 단위 고정

기준선 커밋과 **코드(주석 제외)를 줄 단위로 비교**해 배포 전에 자동으로 막는다.

- 발굴 실행 함수 201줄 · 발굴 API 57줄
- 편집장 69줄 · 채점표 44줄 · 기자규약 68줄
- 검수AI · 검수 레인 673줄
- 답글 잠금 · 품질 게이트 41줄 · URL 독립 줄 35줄
- 카드 트리거 22줄 · 묶음 카드 481줄
- 브리핑 범위 · 고정 틀 236줄
- 유입전환 조회 118줄 · 연결 줄 · 탭별 매출
- 자율 실행 판정 · 화면
- **★발송 하드가드 196줄 · 발송 라우트 61줄**

→ 하나라도 건드리면 **배포 전 감시망에 걸린다.**
→ **대표님 승인 없이 기준선을 옮기지 않는다.**

## 6-4. 자율 vs 승인 경계 — 핵심 원리

**기준 = "되돌릴 수 있나?"**

| | |
|---|---|
| **자율 실행**<br>(대표님 말 = 승인) | 발굴·조회·정리·카드·화면 동작<br>"발굴 돌려"·"보여줘"·"몇 명이야"<br>회사 내부 정보·팩트 — **되돌릴 수 있음** |
| **승인 필수**<br>(반드시 버튼) | 고객 발송(답글·카톡·문자·이메일)<br>결제·계약·삭제<br>외부로 나가는 것 — **되돌릴 수 없음** |

*"저는 버튼을 못 눌러요"* 라고 떠넘기지 않는다 — 내부 동작에서 그 말은 금지다.

## 6-5. 답글 초안 — 결재함 금지

- 답글 초안 = **발굴 리드 하단에 생성 → 대표님이 복사 → 원글에 직접 붙임**
- 승인·반려 대상이 **아니다**
- **★결재함에 절대 안 간다**
- 결재함 = **실제 발송(문자·이메일) 승인용으로만**
- 초안 만들 때 `/api/approval/create` 부르기 **금지**

## 6-6. 밤샘 발굴 — 서버 자동

- 서버가 밤에 자동 발굴 (**PC 꺼도 됨** · 방식 B)
- Render Cron / cron-job.org — 새벽 2시 (`0 17 * * *` UTC)
- `CRON_SECRET`으로 보안 (열쇠 값은 대표님만 아신다 — 코드엔 없다)
- 밤샘 프로필 = **Firestore** → 배포해도 유지
- **★발송 절대 0** (발굴·조회만 — 그 파일엔 발송 코드가 아예 없다)

**리드 배분** — 직업별로 **딱 1회 발굴 → 그 직업 사람들에게 나눔**
(대표마다 따로 돌리면 API 10배 + 한 고객에게 답글 10개 = 민원)

- **재무·보험:** 교육생 각 **10개**, 나머지는 전부 **오대표**
- **다른 직업:** 켠 사람 수로 **1/n**
- 라운드로빈 + 매일 시작 순서 한 칸 회전 (1등 독식 방지)
- 배분 기준 = **검수 이전(발굴 원본)** · 핫 = 상위 20개 안에서 실제로 핫인 것만

## 6-6-2. ★★배포 전 회귀 확인 의무 (2026-07-27 확정 — 반복 실패 방지)

**문제:** 새 기능을 넣을 때마다 기존 기능이 깨졌다. "시험 통과·22블록 보존"이라 보고했는데 실제로는 깨졌다.
**원인:** 시험이 **새 기능만** 보고 기존 전체를 안 봤다. (그날 로그아웃 수정이 **캘린더를 깼다**)

### 1) 배포 전 = 핵심 기능 전체 회귀 확인 (하나라도 깨지면 **배포 금지**)

```
□ 로그인 / 로그아웃 (교육생 포함)   □ 캘린더 조회 ★자주 깨짐
□ 발굴 (7채널·검수)                 □ 시트 조회 / 수정 ★
□ 답글 초안 (발굴 하단)             □ 명단 조회
□ 카드 (명단·여러 명·만기)          □ 밤샘 발굴 (밤샘 켠 대표 수)
□ 보여줘 (핫리드·매출)              □ 유입전환 매출
```

→ 점검 도구: `node deploy/_regression_check.js` (로컬) · `node deploy/_regression_check.js --live` (배포 후)

### 2) "시험 통과"만으로 배포 금지

새로 만든 것만 통과했다고 배포하지 않는다. **실제 기능 전체**를 확인한다. 특히 **캘린더·시트 연동(권한)**.

### 3) 권한·토큰을 건드리는 수정 = 특별 주의

로그인·로그아웃·토큰·세션을 고칠 때는 **캘린더·시트 권한이 안 깨지는지 반드시 확인**한다.
★교훈: 로그아웃을 "진짜로 지워지게" 고쳤더니, 재로그인 때 **물려받을 이전 세션이 없어져** 권한(scope)이 `openid email profile`로 좁아지고 캘린더·시트가 통째로 막혔다.
→ **권한은 이메일 기반 영속 저장소(Firestore)에서 항상 복원**한다. 세션 승계에 의존하지 않는다.

### 4) 회귀를 발견하면

즉시 **원복하거나 수정**한다. **"됐다"고 보고하기 전에 실제로 확인**한다.

## 6-7. 만들 때 지킬 것 (사고에서 배운 것)

- **진단 창구는 실제와 같은 함수를 탄다.** 진단이 "된다"는데 실제로 안 되면 **진단이 거짓말한 것**이다.
- **되는 척 금지.** 실행 안 했으면서 했다고 말하지 않는다. 실패를 "0건"으로 둔갑시키지 않는다.
  "띄울게요" 약속만 하고 안 띄우지 않는다 — **한 일을 세어 센 만큼만** 말한다.
- **보고는 실제 브라우저 화면으로 확인한 것만.** 못 한 건 *"여기서 안 됩니다"* 라고 정직히.
- **시험은 실물 구조 그대로.** 조건이 다른 시험은 통과해도 뜻이 없다.
- **개인정보:** 서버 저장은 주소·토큰·**sha256 지문**뿐. 발굴 리드는 공개글의 채널·링크·발췌(90자)·점수만 —
  작성자·이름·연락처 칸은 **아예 만들지 않는다**.

## 6-8. ★★"통과했는데 실제로는 안 됨" 6가지 함정 (2026-07-29 확정 — 하루에 6번 당함)

**하루 종일 "시험 통과"라고 보고했는데 대표님 화면은 계속 깨져 있었다. 원인은 매번 달랐다.**

### ① "고쳤습니다" ≠ "배포됐습니다"

로컬에서 고쳐도 **병합·배포 안 하면 대표님 화면은 옛날 그대로**다. 폰트·표지·약관 **3번 다** 이 함정에 빠졌다.
→ 배포 확인은 **`/health`의 `commit` 값으로 못 박는다**(서버가 직접 답한 값. 추측·시간 경과로 판단 금지).
→ 보고할 때 **"고침"과 "배포됨"을 반드시 갈라** 말한다.

### ② 시험이 흔들리면 "운"이 아니라 **버그 신호**

E2E가 가끔 실패 → LLM 표현차로 보고 **판정을 넓혔다**. 진짜 원인은 **답변 잘림(max_tokens 부족)**이었다.
넓힌 판정이 **버그를 가릴 뻔했다.** → **흔들리는 시험은 넓히지 말고 원인을 판다.**

### ③ "함수 시험 통과" ≠ "실제 대화에서 작동"

함수 61/61인데 대화에선 안 됐다. **함수 직접 호출은 대화 라우터·activeSkill을 안 거친다.**
→ **E2E 필수:** `node deploy/_test_order_e2e.js` — **서버를 실제로 띄우고 `/api/order`로 사용자처럼** 묻는다.
→ **"렌더링 자리에서 실제로 불리는가"까지** 시험한다(함수 존재만 확인하면 소용없다).
→ ★시험에 **없는 값(가짜 스킬 코드)** 을 넣으면 그 분기가 아예 안 켜져 **통과로 착각**한다. 실제 키를 확인하고 쓴다.

### ④ 텍스트 추출 통과 ≠ 화면에 정상 표시

`pdf-parse`는 ToUnicode 표만 본다 → **폰트 깨짐(렌더링)을 못 잡는다.**
→ **폰트 임베드까지 검사**(`NanumGothic`·`FontFile2`·`CIDFontType2`·`Helvetica 0건`) + `getScreenshot()`으로 **그림 만들어 눈으로 본다.**

### ⑤ 화면 상태(activeSkill)가 **영구히 남아** 질문을 가로챈다

카드 한 번 누르면 `localStorage`에 저장되고 **브라우저를 껐다 켜도 복원**된다. 지우는 건 **[새 대화] 버튼뿐**.
→ 새 기능을 라우터에 넣을 때 **activeSkill 분기의 예외 조건에 넣었는지** 확인한다(현재 예외: `_toolIntent`·`_yakAsk`).
→ 사용자가 "엉뚱한 비서처럼 답한다"고 하면 **[새 대화]를 눌러보게 하면 바로 갈린다.**

### ⑥ 진단 로그는 **결론까지** 찍는다

`match=` 값이 두 뜻으로 보여 **CTO도 오독**했다. 로그가 해석을 요구하면 진단이 틀어진다.
→ `[🧭라우팅★약관은여기] … ★약관질문=true → 📄약관창고로 보냄` 처럼 **결론 한 줄**로 끝낸다.

---

## 6-9. ★★조건이 안 먹으면 "0명"이 아니라 **"전체"**가 된다 (2026-07-31 확정 — 발송 사고 직전)

**사고:** 생일 축하 문자의 결재 대상이 **명단 전체(80명)**로 잡혔다. 실제 8월 생일자는 **1명**.
**원인:** 발송 대상 고르기가 **글자 부분일치**뿐이었다. 생년월일이 `1968-08-17`이라 "8월"로는 못 골랐고,
→ 조건이 **통째로 무시**되어 필터가 없는 것과 같아졌다 = **전체가 대상**.

### ★이것이 무서운 이유

거르기가 실패하면 보통 **0건**이 될 것 같지만, 실제로는 **전부**가 된다.
**"조건을 못 알아들었다"가 "모두에게 보낸다"로 뒤집힌다** — 발송에서는 이게 곧 사고다.

→ **거르는 코드를 짤 때는 "조건을 못 쓰면 어떻게 되나"를 반드시 확인한다.**
   못 쓰면 **넓히지 말고 멈추거나 0으로** 둔다.
→ **대상 수는 화면에 항상 보이게** 한다(대표님이 "왜 80명이지?"로 잡아내신 것이 바로 이것).
→ **미리보기에 보이는 사람 = 실제 나가는 사람.** 두 곳이 **같은 필터 함수**를 쓰게 한다(따로 두면 반드시 어긋난다).
→ 두뇌(명단 검색)가 이미 월·범위·나이를 고를 줄 아는데 **발송만 옛 방식**이었다 —
   **같은 일을 두 군데서 따로 만들면 한쪽만 낡는다.**

### 시험은 말이 아니라 **숫자와 이름**으로

"좁혔어요"라는 답변은 증거가 아니다. `deploy/_test_approval_filter.js` 원칙:
- **몇 명**이 잡혔나 + **누가** 잡혔나 (숫자만 맞고 사람이 틀리는 것도 사고)
- **대상수와 실제 이름 수가 같은가** (숫자만 맞는 착시 차단)
- 아무도 없으면 **0명이라고 정직히** (억지로 채우지 않는가)

### ★시험·측정을 믿기 전에 — **대조군부터**

#### ① 시험이 실패하면 "내 수정 탓인가"부터 가른다

패치를 넣고 결재 시험이 3개 실패했다. **패치 전 상태로 되돌려 같은 시험을 돌리니 똑같이 실패**했다.
→ 원인은 **낡은 시험**(발송 하드가드가 나중에 생겼는데 시험이 그대로였다). 오히려 **하드가드가 잘 막고 있다는 증거**였다.
→ **`git stash` 로 되돌려 한 번 돌려보면 5분에 갈린다.** 안 가르면 멀쩡한 패치를 의심하며 하루를 쓴다.

#### ② 측정값이 이상하면 **그것을 끄고** 다시 잰다

홀로그램 fps가 1로 나왔다. **홀로그램을 숨기고 재도 1**이었다 → 홀로그램 탓이 아니라
**백그라운드 탭에서는 브라우저가 애니메이션을 멈추기 때문**(측정 환경 문제).
→ 진짜 부하는 **그리는 데 걸린 시간**으로 쟀다: 한 프레임 1.12ms = 60fps 예산의 6.7%.
→ **숫자가 나쁘면 먼저 "측정이 맞나"를 의심한다.** 대조군 없는 숫자로 판정하지 않는다.

#### ③ 롤백 해시는 **지시받은 값이 아니라 실제 직전 커밋**을 확인한다

"롤백은 29c35bd" 지시를 받았지만, 그사이 **다른 엄마가 푸시**해 실제 직전은 `2cae3c6`이었다.
지시대로 되돌렸으면 **남의 작업까지 지웠다.**
→ 푸시 직후 `git log --oneline -3` 으로 **부모 커밋을 눈으로 확인**하고, 다르면 **보고할 때 정정**한다.
→ 커밋 전 `git diff --cached --stat` 로 **남의 파일이 안 섞였는지** 확인한다(6-10 ⑦과 같은 병).

---

## 6-10. ★★두뇌를 묶어두지 마라 — 2026-07-31~08-01에 배운 것

**한 줄 원칙: 두뇌(LLM)는 이미 똑똑하다. 사고는 항상 "두뇌의 손발을 묶어둔 코드"에서 났다.**
대표님 성배 — *"어떤 도구를 붙이더라도 속도는 느릴지언정 똑똑한 것은 양보하면 안 된다."*

### ① 낱말표로 의도를 고르면 반드시 샌다

같은 뜻인데 낱말 하나로 갈렸다. **"생년월일 8월인 사람" ✔ / "생일이 8월인 사람" ✘**
음성도 같은 병 — 정규식 관문에 없는 말("명단 띄워"·"다음"·"결재함 열어")은 **서버에 요청조차 안 갔다**(17문장 중 13개).
→ **관문은 낱말이 아니라 뜻으로**(LLM 판정). 기존 분기는 놔두고 **탈락한 것만 받는 안전망을 뒤에 덧댄다**(B안). 최악이어도 현상 유지라 바닥이 안 뚫린다.
→ 구현: `roster_gate.js`(판정) · `main_server.js`는 갈래 몇 줄만.

### ② 계산·세기는 ★코드가 한다. 두뇌에게 세게 하지 마라

상위 30명만 프롬프트에 주고 "몇 명이냐" 물으니 **53명을 20명**, **23명을 11명**이라 답했다.
→ 조건은 두뇌가 읽고, **거르고 세는 건 코드**. 그러면 인원수 환각이 구조적으로 0.
→ 도구는 **칸을 반드시 지켜야 한다**(`doSearch`). 칸을 무시하고 글자만 찾으면 생일 물었는데 **만기·가입일이 섞인다**.

### ③ ★"했습니다"는 반드시 ★다시 읽어 확인하고 말한다

"반영했습니다" + 전/후 표까지 그려 보였는데 **명단은 한 글자도 안 바뀌어 있었다**(도구를 아예 안 불렀다).
→ **쓰기 뒤 다시 읽어 실제 값으로만 보고**한다. 안 된 칸은 "★반영 안 됨"이라고 밝힌다.
→ 시험에도 **일부러 쓰기를 실패시켜 "반영했다"고 말하지 않는지** 넣는다.

### ④ LLM에게 답을 ★글자로 받지 마라 — 도구(구조화)로 강제

"YES/NO만 답해라" → 모델이 설명을 쓰다 잘려 **빈 답**이 왔다(16개 중 8개 탈락).
→ `tool_choice`로 **참/거짓 칸을 강제**하면 어떤 말투로 생각하든 결과가 항상 잡힌다.
→ 함께 겪은 함정 둘: **`temperature`는 최신 모델에서 폐기**(붙이면 400) · **도구 칸 이름은 영문만**(한글 키는 400).

### ⑤ ★판정이 통째로 죽어도 시험은 ✅ 로 보인다

판정이 전부 400으로 죽자 **"안 끌어감" 시험 13개가 저절로 통과**했다.
→ 시험에 **"오류 0이었나"를 반드시 넣는다.** 안 하면 전부 죽은 걸 성공으로 읽는다.
→ 판정 결과가 **한쪽으로만 쏠리면(전부 NO/전부 YES) 실력이 아니라 고장을 의심**한다.

### ⑥ 사진은 ★읽기와 해석을 갈라야 환각이 안 난다

한 번에 "읽고 해석해" 시키면 모델이 **흐린 글자를 그럴듯하게 메운다**.
→ ①눈은 **글자만 옮겨 적고**(안 보이면 `[안 읽힘]`) ②해석은 **텍스트 해석기 한 벌**이 한다.
→ 옮겨 적은 글을 응답에 함께 돌려주면 **무엇을 보고 그렇게 말했는지** 대표님이 확인하실 수 있다.

### ⑦ 여럿이 동시에 작업할 때 — ★남의 커밋이 내 파일을 데려간다

엄마 1의 커밋에 **내 `main_server.js` 줄이 딸려 들어갔는데** 그 줄이 부르는 모듈은 **아직 커밋 전**이었다.
→ 그대로 푸시됐으면 **서버가 부팅에서 죽는다.** 다행히 푸시 전에 잡았다.
→ **새 모듈을 만들면 그것을 부르는 줄과 ★같이 커밋**한다. 공용 파일(`main_server.js`·`genya.html`)은 **한 사람씩 직렬로**.

---

## 6-11. ★2026-08-02 — 신청 캡처·시뮬레이터 실전 배선에서 배운 것

**오늘 라이브된 것**
- 📥 **신청 캡처** (`c796c66`) — 공개 폼 **`/apply`** → 「신청수집」 시트 9칸. `apply_sheet.js` 독립 모듈.
  **`rep_id = 'r' + sha256(이메일) 앞 5자`** — 저장하는 표가 없어 재배포해도 같은 값이고, **이메일이 공개 주소에 안 뜬다**(복원 불가). rep 없으면 `unassigned`(거부도 대표 귀속도 아님).
- 🔌 **시뮬레이터 실전 배선** (`1649b6c`) — ①원고 = 진짜 엔진(`/api/promo2/expand12`) · ②콘텐츠 = **기존 함수 그대로 호출**. **③채널·④배포·분석 대시보드 숫자는 아직 그림**이다.

### ① 시트를 만드는 건 ★사람이다 — 서비스계정은 못 만든다

SA 권한이 `drive.readonly`라 **새 시트를 생성하지 못한다**(`service_auth.js`). 대표님이 만들고 **SA 이메일에 편집자 공유 + 환경변수 등록**해야 비로소 돈다.
→ `PROMO_SHEET_ID`·`APPLY_SHEET_ID` **두 번 연속 같은 병목**이었다. **코드를 다 짜도 시트가 없으면 멈춘다** — 새 시트가 필요하면 **맨 먼저** 요청한다.
→ 라이브가 안 되면 SA를 의심한다: **로컬 키와 Render 키가 다른 계정일 수 있다**(오늘 실제로 그랬다 — "The caller does not have permission").

### ② 상태 코드만 보면 시험이 거짓말한다

400 세 개가 나와 "전부 막힘 확인"으로 볼 뻔했다. 진짜 원인은 **PowerShell이 한글 본문을 UTF-8로 안 보내** `'상담'`이 깨진 것 — **검증이 아니라 하네스가 고장**이었다.
→ **오류 "문구"가 서로 다른지까지 본다.** 네 가지 실패가 네 가지 다른 말을 해야 판정이 살아 있는 증거다(6-10 ⑤와 같은 병).

### ③ 시험 줄 이름이 같으면 **지난 회차**를 집는다

같은 이름으로 시트에 쌓다 보니 `find()`가 **이전 실행의 줄**을 집어, 멀쩡한 코드가 **1회차만 통과**하는 흔들리는 시험이 됐다.
→ 시험 데이터에 **회차 고유값**을 넣는다. 흔들리면 **넓히지 말고 원인을 판다**(6-8 ②).

### ④ ★한글 파일을 PowerShell로 고치지 마라

`Set-Content -Encoding UTF8` 한 번에 **파일 전체가 깨졌다**(주석·문자열 전부 깨짐).
→ **한글이 든 파일 수정은 Edit 도구로만.** PowerShell은 읽기·실행에만 쓴다.

### ⑤ 롤백 지점은 **하루에 두 번도 움직인다**

지시받은 `dc13442`로 되돌렸으면 그 뒤에 들어온 **`2fcf467`(시뮬레이터 진짜판 교체)이 지워졌다.** 6-9 ③ 재확인.
→ **브랜치도 남이 갈아탄다.** 작업 중 브랜치가 `voice-stop-hard`로 바뀌어 **내 커밋이 남의 브랜치에 떨어졌다** → `cherry-pick`으로 옮기고 남의 브랜치는 `git branch -f origin/…`으로 원복했다.
→ 커밋 전 **`git status -sb`로 지금 어느 브랜치인지**, `git diff --cached --stat`로 **남의 파일이 안 섞였는지** 본다.

### ⑥ 공개 쓰기 라우트 — 이 서버의 첫 사례

`POST /api/apply/submit`은 **로그인 없이 누구나** 부른다.
→ **검증은 전부 서버가 한다.** 화면 검사는 편의일 뿐 최종 판정이 아니다 — **폼을 건너뛰고 나쁜 값을 직접 던져도 막히는지** 시험한다(실제로 그렇게 시험했다).
→ 방어: 허용목록(관심 3종)·폭주 차단(1분 5회)·10분 중복 1줄·글자수 상한·rep 소독.
→ **★남는 위험: 캡차가 없다.** 작정한 봇은 못 막는다 — 쓰레기가 쌓이면 그때 캡차를 올린다.

### ⑦ 새로 만들지 말고 ★있는 것을 불러라

시뮬레이터 ②콘텐츠는 `genya.html`에 **이미 있던** `_whCardBytes`·`whBlogHtml`·`_whZip`·`_whDownloadBlob`을 **`window.parent`로 그대로 부른다**(같은 서버가 주는 iframe이라 가능).
→ **그리기·압축 로직을 복사하지 않았다.** 복사하면 두 군데가 되고 한쪽만 낡는다(6-9와 같은 원칙).
→ 부를 함수는 **이름을 추측하지 말고 실물로 확인**한다(오늘 8개 전부 줄 번호까지 확인 후 붙였다).

---

## 6-12. ★2026-08-03 — 여러 엄마가 동시에 배포한 날 배운 것

**한 줄 원칙: 혼자 일할 때 안 나던 사고가 ★여럿이 동시에 일하면 난다. 그리고 "확인했다"는 말이 틀릴 수 있다.**

### ① 남의 스테이지가 내 커밋에 딸려 들어간다

`git add`로 파일 3개를 지정했는데 커밋에는 **6개**가 들어갔다. 다른 엄마가 미리 `git add` 해 둔 것이 스테이지에 남아 있었다.
그대로 푸시돼 **남의 미완성 작업이 내 브랜치에 실려 나갔다**(다행히 별도 브랜치였다).
→ 커밋 전에 **`git reset`으로 스테이지를 비우고**, **파일 이름을 지정해 add** 한다. `git add -A` 금지.
→ **`git diff --cached --stat`을 눈으로 보고** 커밋한다. 6-10 ⑦의 재발이다.
→ 이미 섞였으면 `git reset --soft HEAD~1` → 남의 파일만 `git restore --staged` → 다시 커밋. **남의 작업은 작업트리에 그대로 남는다**(안 잃는다).

### ② 롤백 지점 지시가 ★틀릴 수 있다 (하루에 두 번)

`5072618`·`e801b54`로 지시받았지만 실제 직전은 각각 `e801b54`·`b34c7d4`였다.
그대로 되돌렸으면 **RAG(2,766청크)와 진입관문 수정이 통째로 지워졌다.**
→ 원인은 거짓말이 아니라 **시차**다. 여러 엄마가 동시에 배포하면 지시하신 시점과 실제 HEAD가 어긋난다.
→ **`git log --oneline origin/main -3`으로 실제 부모를 눈으로 확인**하고, 다르면 **보고할 때 정정**한다(6-9 ③).

### ③ 라이브 점검에서 "없음"이 나오면 ★검사식부터 의심

배포 확인 스크립트가 "롱폼 ★없음"이라 답했다. 실제로는 **정상 반영**돼 있었고 **내 정규식이 틀린 것**이었다(속성 순서를 잘못 잡았다).
→ 그대로 믿었으면 멀쩡한 배포를 되돌릴 뻔했다.
→ **"없음"은 두 가지 뜻이다** — 정말 없거나, 내가 잘못 찾았거나. **다른 방법으로 한 번 더** 확인한다(파일에서 직접 세기 + git에서도 확인).

### ④ 자료를 주입하는 기능은 ★A/B로 검증해야 한다

`[+ 내 자료]`가 프롬프트에 들어가는지는 단위 시험 40/40으로 통과했다. 그런데 **진짜 엔진으로 A/B를 돌리니** 두 가지가 드러났다:
쇼츠처럼 글자 수가 빠듯한 종류에서 ①**주제가 자료 쪽으로 넘어가고** ②**자료의 서식(`**[1씬]**` 별표)까지 따라 했다.**
→ **문자열 검사만으로는 이 실패가 안 보인다.** 자료가 "재료"로 쓰이는지 "주제"를 갈아치우는지는 **결과물을 비교해야** 안다.
→ 시험 설계: **한줄카피 주제와 자료 주제를 일부러 다르게** 둔다. 주제가 유지되면 재료로 쓴 것이다.
→ RAG 문턱도 같은 방식으로 정했다(재무 질의 0.405~0.654 / 무관 질의 0.330~0.338 → **문턱 0.40**). **문턱을 낮추면 무관한 내용 5,696자가 딸려 들어와 주제를 흐린다.**

### ⑤ git 잠금 잔재가 merge를 통째로 막는다

`.git/AUTO_MERGE.lock`·`packed-refs.lock`(둘 다 0바이트·같은 시각)이 남아 **`git merge`가 44분간 전부 실패**했다. 커밋·푸시는 됐다.
→ **잔재인지 가르는 법**: `MERGE_HEAD`가 없고 · 돌고 있는 git 프로세스가 0개면 잔재다.
→ 지우기 전에 **`git worktree list`로 다른 작업장을 확인**하고, **그 파일 하나만** 지운다. worktree 폴더는 건드리지 않는다.
→ 잠금이 살아 있어도 **브랜치 푸시(`git push origin 브랜치:main`)로 우회**할 수 있다 — 배포가 막히지 않는다.

### ⑥ 라이브에서 ★내가 확인 못 하는 것이 있다

`/api/promo2/*`는 **전부 로그인 게이트**다. 진단 창구(`/diag`)도 마찬가지다.
→ RAG가 라이브에서 실제로 도는지는 **내가 못 본다.** 로컬에서 같은 코드·같은 Pinecone으로 검증한 것까지가 내 몫이다.
→ **"확인했다"와 "대표님이 확인하셔야 한다"를 갈라서** 보고한다. 못 본 것을 본 것처럼 말하지 않는다.
→ 대신 **간접 증거를 찾는다**: `/health`의 `pineconeReady`는 `PINECONE_API_KEY && OPENAI_API_KEY`를 본다 → `true`면 RAG가 쓰는 두 열쇠가 다 있다는 뜻이다.

### ⑦ 비용 드는 API는 ★차단됐으면 호출조차 하지 않는다 (CTO)

교육생을 막을 때 **응답만 막고 호출은 하면 돈은 이미 나간다.**
→ 게이트를 **호출 앞에** 둔다. 막힌 사용자는 API가 **아예 안 불린다.**

### ⑧ 게이트는 ★서버가 판정한다 (CTO)

화면에서 버튼을 숨기는 것은 게이트가 아니다. 개발자도구로 함수를 직접 부르면 뚫린다.
→ **VIP 판정·비용 판정은 서버에서** 한다. 화면 게이트는 편의일 뿐 최종 판정이 아니다(6-11 ⑥과 같은 원리).

### ⑨ OAuth를 프로덕션 → 테스트로 되돌리면 ★세 가지가 동시에 터진다 (CTO)

① 테스트 유저 목록이 날아가고 ② refresh token이 **7일 만료**로 바뀌고 ③ **머니야까지 영향**받는다(OAuth 프로젝트 `moneya-72fe6` 공용).
→ **게시 상태 = 프로덕션 유지.** 스코프·게시 상태는 대표님 승인 없이 절대 안 건드린다.

---

## 7. 문서 구조

이 파일(CLAUDE.md)은 **짧게 핵심만** 담는다. 깊은 내용은 백서에 둔다.

**모든 작업 전에 docs와 agents 폴더를 함께 읽는다.**

**대표가 "오늘 일 정리하자"고 하면 docs/진행기록.md에 그날 한 일과 다음 할 일을 자동으로 기록한다.**

- `docs/` — **프로젝트 백서(영구 기억 저장소).** `00_백서_개요.md`가 목차. 여기 적힌 정의·원칙은 임의로 바꾸지 않는다. 작업 전 관련 백서를 펼쳐 읽는다.
- `agents/` — 지니야 시스템 프롬프트 모음.

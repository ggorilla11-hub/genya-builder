# 지니야빌더 밤샘 자율개발 기록 (2026-07-19 야간)

> 대표님 취침 중 자율 개발. 아침에 코드 비교(코치 vs 엄마). 정직·실동작·가짜0 원칙.
> 대원칙: deploy/ (배포=커밋 842ebb8). 각 건 → node --check → commit(F번호) → push → 이 기록 실시간.
> 법률: 제로인그레스(서버저장0)·휴먼인루프(승인후발송)·멀티테넌트 격리 유지.
> ⛔ 카톡 WM_CHAR·kakao_watcher zip은 손대지 않음(아침에 대표님 실증 후).

## 작업 목록 (대표님 지시 순서)
- [ ] **F-1** 홍보마케팅비서 좌측 삭제 (좌측 중복 제거, 우측 openWarehouse는 유지)
- [ ] **F-2** 비서 모달 여백 통일 (_bizShell padding → pop-h 기준으로 넉넉히)
- [ ] **F-3** 문자(SMS) 실발송 (POST /api/send/sms, solapi, 휴먼인루프·제로인그레스)
- (이후 지시 도착 시 추가)

---

## 진행 로그

### F-1 ✅ 홍보마케팅비서 좌측 삭제 (완료)
- 수정: deploy/genya.html 722줄 `<div class="tool" onclick="openWarehouse()">홍보마케팅비서</div>` 삭제.
- 유지: 721 "마케팅 도구" 섹션 제목, 723 유튜브 발굴, 우측 openScenario megaphone(1402), openWarehouse 함수(1697) 전부 무접촉.
- 검증: grep으로 openWarehouse 참조 확인 — 좌측 항목만 제거, 우측 호출부·함수 정의 그대로. HTML이라 node --check 비대상 → 단일 self-closed div 삭제라 구조 무손상.
- 커밋: F-1 / push 완료. (708b65b..c37ce13)

### F-2 ✅ 비서 모달 여백 통일 (완료)
- 원인 확정: `.pop` 박스(genya.html:244)에 padding 없음 → `_bizShell`(1804) padding:2px가 유일 여백이라 답답. 닫기 ×는 top/right:-2px로 밖으로 삐져나감.
- 수정1 `_bizShell`(1804·1805): 컨테이너 padding 2px → **22px 24px**(pop-h 18/28 기준 넉넉히), 닫기 × top:-2px right:-2px → **top:14px right:16px**(여백 안으로), × 40→36px. → 리마인더·보상·결재함·발송기설치 4개 모달 공통 적용.
- 수정2 발굴(`_prospectHTML`:1193): `_bizShell` 미사용 별도 구조라 컨테이너 `max-width:560px`에 **padding:22px 24px** 추가. 탭 UI·헤더·× 기능 무접촉.
- 검증: 발굴·리마인더·보상·결재함 4개 모달 여백 확보. .pop max-width calc(100vw-40) → 폰(375px) 콘텐츠폭 ~287px 확보. 닫기·기능 로직 그대로.
- 커밋: F-2 / push. (c37ce13..dce9a0e)

### F-3 ✅ 문자(SMS) 실발송 (완료 — 코드 실동작, 실키 검증만 대표님)
- 서버 `main_server.js` POST `/api/send/sms` 추가(solapi/save 라우트 뒤):
  - memberAuth+hasDataScope 확인 → 없으면 needsConnect. 회원 본인 시트 `지니야_연결`(A=라벨,B=값)에서 솔라피_API_KEY/SECRET/발신번호 읽음(서버 저장 0·멀티테넌트 격리).
  - solapi v4 정품 인증: crypto(내장) HMAC-SHA256(date+salt, secret) → POST https://api.solapi.com/messages/v4/send {message:{to,from,text}}. fetch는 Node18 내장(package.json engines node>=18).
  - ★가짜성공 금지: statusCode 2000 / SENDING / messageId 일 때만 sent:true. 아니면 solapi 사유 그대로 반환. ★제로인그레스: to·text는 발송 요청에만, 서버 저장 0. ★휴먼인루프: 웹 [승인] 후에만 호출.
- 프론트 `genya.html`: 공유/발송 모달 문자 항목 `toast(준비중)` → `shareSms()`. shareSms/shareSmsSend 추가(카톡 폼과 동일 패턴, 승인→fetch /api/send/sms, needsConnect/needsSolapi/실패 정직 토스트, 발송중 버튼 잠금).
- 검증: node --check main_server.js ✅ / genya.html 인라인 JS 추출 후 node --check ✅(shareSms 포함).
- ★정직 한계: 코드는 실제 solapi API를 정확한 서명으로 호출하는 진짜 동작 코드(mock·하드코딩 없음). 단 실제 문자 도달 end-to-end는 회원의 실제 솔라피 키+발신번호(사전등록·유료) 필요 → 브라우저·실키 없는 야간엔 미검증. "코드상 여기까지 = 실호출 완성, 실발송 검증은 회원 실키로 대표님/회원 실측".
- 커밋: F-3 / push. (dce9a0e..e30c8c5)

---

## 배포 반영·라이브 실측 (야간, 로그인 불필요 범위)
- **배포 무결성**: 3커밋 push 후 genya-builder.onrender.com **HTTP 200 정상**. F-1(홍보마케팅 좌측 0건)·F-3(shareSms 함수·문자항목 연동) 배포 반영 확인. 우측 openWarehouse 함수 유지. **사이트 안 깨짐.**
- **① 모델·키 실작동(대표님 F-4 걱정)** ✅ — `POST /api/onboard/chat` 실호출 → 지니야 실응답 수신 = **ANTHROPIC_API_KEY + claude-sonnet-5 배포에서 실동작 확인**. 페르소나(클로드 미언급) 정상. → 증권/연금/상품비교 4종 엔진(모델·키) 살아있음 실증. (남은 것: 실제 증권 사진 업로드 end-to-end는 로그인 필요 → 대표님 실측)
- **② `/api/status`** ✅ — openai/약관RAG/googleOAuth/skills=active, kakaoLogin=no-key(F-5 일치), solapi="회원 키 저장 시"(F-3 일치), calendar/sheets/drive="회원 연결 시".
- **③ F-3 라우트 배포·게이트** ✅ — `POST /api/send/sms` 미로그인 → `needsConnect` 정상(실발송 0). 게이트 실작동.

## 야간 최종 요약 (아침 확인용)
- ✅ 완료·배포: **F-1**(홍보마케팅 좌측 중복 제거) · **F-2**(비서 모달 여백 통일) · **F-3**(문자 SMS 실발송, 솔라피 실연동).
- ✅ 사이트 HTTP 200 정상, 모델·키 실작동 실증.
- 🧑 대표님 아침 실측 남은 것: (1)카톡 WM_CHAR 가족1명(배포zip은 실증 후 대표님 push) (2)문자 실발송은 회원 실제 솔라피 키+발신번호 등록 후 1건 (3)증권 사진 업로드 end-to-end.
- ⛔ 안 건드림: 카톡 core WM_CHAR·kakao_watcher zip(대표님 아침 실증 후).
- 지시받은 F-1~3 외 임의 코드 변경 없음(추측 금지 원칙 유지).

---

# 2차 세션 — 시연 매뉴얼 2부 "미해결" 해결 (대표님 재지시)

> 매뉴얼 2부(F-1~F-16) 중 아직 안 된 것만. 규칙 동일: node --check → commit(F번호) → push → 실시간 기록. 카톡 core·zip 무접촉.
> 분류: 이미해결(2부 F-1·2·4·8) / 코드가능(F-3·F-12·F-9·F-10) / 대표님 env(F-5·6·7) / 중규모·외부의존(F-11·14·15·16).

### 2부 F-3 ✅ 다운로드함 "열기" 라벨 정직화 (완료)
- 확인: `_vaultOpen`(genya.html:2178)은 pushMsg로 **요약 텍스트만 재표시**(파일 안 열림) — 매뉴얼대로.
- 수정: 목록 버튼 라벨 `열기` → `요약 보기`(genya.html:2170). 오해 제거(거짓 "됨" 방지). 로직 무변경.

### 2부 F-12 ✅ 증권 PDF → 예쁜 리포트 경로 (완료·실측 대표님)
- 원인 재확인(코드): 프론트 2662 폴백이 policy+비이미지(PDF)를 coverage(평문)로 우회. 그러나 `analyzePolicy`는 PDF를 **document 블록으로 직접 처리하는 분기가 이미 있음**(policy_analysis_skill.js:216-217), `/api/policy`는 images 그대로 전달(main_server.js:613), 정상경로(genya.html:2675)는 policy면 /api/policy로 전송 → **주석 2659 "PDF 400"은 옛 정보로 틀림**.
- 수정: 폴백 조건에 `&& window._ACTIVE_SKILL!=='policy_analysis'` 추가(genya.html:2662) → 증권 PDF도 폴백 안 타고 /api/policy(document 처리) → 예쁜 HTML 리포트. 주석 정정.
- 안전성: skill의 PDF document 처리·크기가드(20MB)·unreadable 정직표시·실패 시 우아한 폴백 모두 실재 확인. 회귀 위험 낮음.
- ★실측: 증권 PDF 1장 업로드 end-to-end는 로그인 필요 → 대표님 실측. (시연은 사진 권장 유지)

### 2부 F-9/F-13 ✅ 이메일(Gmail) 실발송 (완료 — 코드 실동작, 실계정 검증만 대표님)
- 서버 `main_server.js` POST `/api/gmail/send`(SMS 라우트 뒤): gateGoogle→회원 oauth client, `gmail.compose` 스코프(1129)로 `gmail.users.messages.send`. 한글 제목=MIME encoded-word, 본문=UTF-8 base64, raw=base64url. ★가짜성공 금지: messageId 반환 시만 sent:true. ★제로인그레스: 받는이·제목·내용 서버 저장 0. ★멀티테넌트: 회원 본인 토큰. scopeGate로 미연결 시 needsConnect.
- 프론트 `genya.html`: 공유/발송 이메일 항목 `toast(준비중)` → `shareEmail()`. shareEmail/shareEmailSend 추가(SMS 폼과 동일, 받는이·제목·내용 → 승인 → /api/gmail/send, needsConnect/실패 정직 토스트).
- 검증: node --check main_server.js ✅ / genya.html 인라인 JS ✅.
- ★정직 한계: 실제 메일 도달은 회원 Gmail 연결 필요 → 대표님/회원 실측.

### 2부 F-10 ✅ 결재함 승인 후 실발송 실행부 (완료)
- 원인: apApprove(genya.html:1967)가 상태만 '승인'으로 바꾸고 실발송은 "트리거 자리"(1971, 실발송 0).
- 수정: apApprove에 실행부 추가 — 결재건에 발송정보 `item.send={channel:'kakao'|'sms'|'email', to, subject, text}`가 있으면 승인(=사람 관문) 시 해당 채널로 **실제 발송**: 카톡=_genyaSilentSave(로컬 발송기)·문자=/api/send/sms·이메일=/api/gmail/send(F-8·F-9 재사용). 발송정보 없는 결재건(서류 정리 등)은 승인 기록만. → 휴먼인루프 파이프라인 완성.
- ★정직 범위: 실행부는 완성(발송정보 담긴 결재건은 승인 즉시 실발송). 현재 결재함에 올라오는 리마인더 마감·보상 서류 item은 발송 대상이 도메인상 불명확(회사 재촉/보험사 제출)해 send 미지정 → 승인 기록만. 발송 대상·내용이 명확한 결재건에 send 필드를 담아 _approvalPush하면 즉시 실발송된다. (가짜 성공 없음: 각 채널 응답으로 성공/실패 정직 토스트)

### 2부 F-11 ✅ 보상 회사양식 자동채움 (증빙 읽기 → 양식 채운 초안으로 격상)
- 이전: 증빙 첫 장만 /api/coverage/analyze로 읽고 "회사 양식 자동 매핑은 준비 중" 하드코딩(genya.html:1925).
- 서버 `main_server.js` POST `/api/claim/build`: 양식+증빙(여러 장)을 함께 content 배열(image/document)로 구성 → `_anthropic.messages.create`(WS_CHAT_MODEL). 프롬프트: 양식 항목을 증빙 값으로 "항목: 값" 채움, 없으면 [확인 필요](지어내기 금지), 손해액·적정성 판단 금지, "제출 전 검토" 명시. ★제로인그레스(메모리만).
- 프론트 `genya.html` claimBuild: 증빙 첫 장만 → **양식+증빙 전체** FileReader로 읽어 /api/claim/build 전송. _claimResult 문구 "증빙에서 읽은 내용/자동매핑 준비중" → "양식에 채운 청구서 초안 · [확인 필요] 제출 전 검토"로 정직화.
- ★정직 한계: 양식을 이미지/PDF로 읽어 항목을 증빙 값으로 채운 **텍스트 초안** 생성(실동작). 원본 양식 위에 그대로 겹쳐 채운 PDF 오버레이는 양식 좌표 파싱이 필요해 별도(추후). 회사 양식 다양성상 [확인 필요] 항목은 사람이 확정. 실측은 대표님(실제 양식+증빙).

---

## 2차 세션 — 남은 F 정직 판단 (코드만으로 온전한 실동작 불가 → 보류/대표님 몫)
- **F-5 카카오 로그인** = `KAKAO_REST_KEY` 미설정 → **대표님이 Render env 설정**. 코드 아님.
- **F-6 발굴(유튜브)** = `YOUTUBE_API_KEY` 미설정 → **대표님 Render env**. 코드 아님.
- **F-7 구글 데이터 연결** = 회원이 OAuth 동의(도구연결) → **운영**. 코드 이미 완성.
- **F-14 홍보 발행 2~4단계** = 대표님 전용 캠페인 파이프라인(jenya 캠페인표 의존) + 3단계 콘텐츠 생성·4단계 SNS 발행은 **외부 대규모(콘텐츠 자동공장 별도 프로젝트)**. 원고(2단계) LLM 변환만 부분 가능하나 시연 무관·반쪽이라 가짜로 안 채움. **정직 보류.**
- **F-15 리마인더 카톡 자동감지** = 카톡 로그 접촉 필요 → **⛔ 카톡 무접촉 원칙(대표님)** 위반이라 안 함. **보류.**
- **F-16 커넥터 배선**(소셜리스닝·웹조사·Gmail커넥터) = 외부 검색 API 키·연동 필요(내가 키 없음). 가짜 하드코딩 금지 원칙상 실동작 불가 → **정직 보류.** (현재 "준비 중" 정직표시가 오히려 신뢰)

## 2차 세션 최종 요약
- ✅ 코드로 실동작 완성·배포: **F-3**(다운로드함 라벨) · **F-9/13**(이메일 Gmail 실발송) · **F-10**(결재함 승인→실발송 실행부) · **F-11**(보상 양식 채운 초안) · **F-12**(증권 PDF 예쁜 경로). + 어제 F-1·F-2·F-8.
- ⛔ 보류(정직): F-14(외부/대표전용)·F-15(카톡무접촉)·F-16(외부키). 대표님 몫: F-5·6·7(env·연결).
- 원칙 유지: 제로인그레스·휴먼인루프·멀티테넌트 격리 / 가짜0 / 카톡 core·zip 무접촉 / 각 건 node --check→commit→push.

---

## 3차 — 코치 코드 대조 반영 (대표님: 코치가 더 나은 부분만 채택)
- 코치 코드 `C:\Users\user\Desktop\main_server (1).js`(116KB) 대조. 대표님 대조 결론: 문자·이메일·결재함은 우리 접근 동일. **단 하나 — 이메일 gmail 스코프.**
- **F-9 보강 ✅ gmail.send 스코프 추가**: 내 CONNECT_SCOPES.gmail = [gmail.readonly, gmail.compose]만(main_server.js:1191) → 코치는 gmail.send 포함(코치파일:1254). gmail.compose도 공식상 send 포함하나, **명시적 gmail.send가 실발송 확실성↑(안전장치)** → 코치 해법 채택. gmail 스코프에 `.../auth/gmail.send` 추가 + 라우트 주석 정정.
- ★주의(대표님 실측): 스코프 추가 = 기존 Gmail 연결 회원은 **재동의(incremental) 필요**. 이메일 발송 실사용 전 [Gmail 재연결] 1회. (아직 실사용 전이라 무영향)
- 검증: node --check main_server.js. 나머지 라우트는 코치와 동일 확인 → 추가 반영 없음(과채택 금지).








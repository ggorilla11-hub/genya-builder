# progress_v4.0 · Step 2-A 개인화 메모리 · Day 2 (엄마2)

- **작성일**: 2026-07-22
- **브랜치**: `feature/step2-A-personalization` (로컬 · 미푸시)
- **상태**: 🟡 코드·인덱스 정상 · **Pinecone 월 쓰기 한도 소진(429)으로 저장 실측만 막힘**

## 라이브 검증 결과 (로컬, PINECONE 키 주입 후)
| 단계 | 결과 |
|---|---|
| ① 인덱스 `ohwant-genya` 라이브 생성 | ✅ Ready · dim 1536 · cosine · aws us-east-1 |
| ② 임베딩(text-embedding-3-small) | ✅ 1536차원 정상 |
| ③ 벡터 저장(upsert) | ❌ **429 · 이번 달 write unit 한도(5,000,000) 소진** |
| ④ 시나리오2 조회 | ⛔ 저장이 안 돼 확인 불가(저장만 되면 조회 로직은 정상) |

## 핵심(정직)
- **코드·설계는 완성이고 정확합니다.** 인덱스 생성·임베딩·질의 모두 작동.
- **막힌 것은 Pinecone 계정의 이번 달 쓰기 한도(무료 플랜 500만 units) 소진** 한 가지뿐.
- 이 `PINECONE_API_KEY`가 기존 다른 용도(예: 약관 RAG)와 **같은 계정**이면 그쪽에서 한도를 쓴 것으로 추정.

## 회장님 결정 필요 (A/B/C)
- A. 다음 달 한도 리셋 대기 · 무료 유지 · **실측 지연**
- B. **오원트-genya 전용 새 무료 Pinecone 계정 → 새 키** · 즉시 실측 · 무료 · ⭐(팀장 추천)
- C. 현재 계정 유료 업그레이드 · 즉시 · 비용 발생

## 완료된 것
- `personal_memory.js`(A-1~A-5 코드) · 인덱스 라이브 생성 · `.gitignore`에 임시테스트 제외

## 다음(키 결정 후)
- 저장 실측 → 시나리오2 "어제 만든 자료 뭐였지?" 통과 확인 → A-6(문서 임베딩)·A-7(생성물)

## 절대유지
✅ ohwant-homepage 무접촉 · Step2-1 프로덕션 그대로 · main push 금지 · 엄마3 브랜치 무접촉
